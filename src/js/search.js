import { getItemFromUrl } from "./utils.mjs";

/* async function searchFromAPI()
{
    var searchText = getItemFromUrl("search");
    var data = Search(searchText);
    var x = 0;
}

searchFromAPI(); */
